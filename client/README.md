# Airbnb Clone - React Client

This is a minimal React frontend that consumes the Express API in the repository.

Run (from `airbnb-clone/client`):

```bash
npm install
npm run dev
```

The client talks to the backend at `http://localhost:3000/api`. Ensure the server is running.
